{
  "buildCommand": "pnpm --filter @workspace/football-pool run build",
  "outputDirectory": "dist/public",
  "installCommand": "pnpm install",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}